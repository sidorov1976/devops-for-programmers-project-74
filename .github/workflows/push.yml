name: project-build

on: push

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: sudo apt install make
      - run: docker run --user $(id -u) -it -w /root -v `pwd`/app:/root node:14.18.1 npm ci
      - run: make ci
